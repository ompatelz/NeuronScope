# NeuronScope backend

FastAPI is the transport layer for bounded NeuronScope experiments. Independent domain packages
generate datasets, construct PyTorch models, train and instrument them, evaluate transparent
diagnostics, and assemble decision-boundary and playback data.

See the [root README](../README.md) for setup, verification, architecture, and product limitations.
