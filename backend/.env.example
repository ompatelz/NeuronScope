# Copy to .env only when local settings need an override.
NEURONSCOPE_APP_NAME=NeuronScope API
NEURONSCOPE_API_V1_PREFIX=/api/v1
# Set only for the single-container production build.
# NEURONSCOPE_FRONTEND_DIST=/app/static
